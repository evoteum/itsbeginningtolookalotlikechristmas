apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: site-nginx
spec:
  podSelector:
    matchLabels:
      app: site
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - ports:
        - port: 8080
  egress: []
